# ============================================
# FIXED Requirements for Windows + GTX 1650
# Tested and compatible versions
# ============================================

# Core ML Libraries (Compatible versions)
torch==2.0.1
transformers==4.30.2
datasets==2.13.1
peft==0.4.0
accelerate==0.20.3

# Utilities
tqdm==4.65.0
requests==2.31.0
safetensors==0.3.1

# Web UI
streamlit>=1.24.0
pygments>=2.15.0

# NOTE: bitsandbytes is EXCLUDED for Windows compatibility
# If you need 8-bit quantization on Windows, use alternative methods
